package com.sist.main;
/*
 *	상속 - 키워드 extends
 *	    - 객체생성 - 하위클래스 = 하위클래스 => 상위클래스를 확장시켰기 때문에 가장 많이 사용
 *	      ex) class A
 *            class B extends A
 *            B b=new B()
 *            A a=new B()
 *            라이브러리 => 형변환
 *      - 상속을 내리는 클래스는 공통성이 많다
 *        ex) class Music => 목록, 순위, 음반, 포인트...
 *              - class Genie extends Music => (목록, 순위, 음반, 포인트...) +a
 *              - class Melon extends Music => (목록, 순위, 음반, 포인트...) +a
 *              - class Mnet extends Music => (목록, 순위, 음반, 포인트...) +a
 *              -------------------------- 추가(오버로딩)/수정(오버라이딩) 가능
 *  객체지향
 *  - 데이터 보호 = 캡슐화
 *  - 기존 클래스 재사용 = 상속
 *  - 추가, 수정이 가능 = 다형성 - 오버로딩(추가), 오버라이딩(수정)
 * 
 */
public class MainClass_6 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
